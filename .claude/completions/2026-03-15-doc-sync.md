# 문서 현행화 (2026-03-15)

> 전체 소스 코드 리뷰 후 BUSINESS_LOGIC.md, FUNCTION_REFERENCE.md 현행화 작업.
> 이전 세션에서 남겨진 오류/누락 항목 전수 수정.

---

## 1. BUSINESS_LOGIC.md 수정

| 섹션 | 수정 내용 |
|------|-----------|
| 섹션 5 강제 손절 흐름도 | `forced_qty=None` → `forced_qty=holding.quantity` (실제 구현 반영) |
| 섹션 5 "알려진 이슈 — 손절 후 반등" | ⚠️ 미해결 → ✅ 수정 완료 (2026-03-15) |
| 섹션 4 split tranche + cooldown | 잘못된 "수정 계획" 항목 제거 — 코드 이미 올바르게 동작 |
| 섹션 4 USD 현금 미차감 허점 | ✅ 구현 완료로 상태 변경 (aa25a27, _deduct_loop_cash) |
| 섹션 6-1 AssetManagementService | 목표비율 BULL/BEAR 수치 수정, 구현완료 헤더로 변경 |
| 섹션 6 플로우차트 | BULL 20% / NEUTRAL 40% / BEAR 50% → BEAR 20% / NEUTRAL 40% / BULL 40% |
| Last Updated | 2026-03-15로 갱신 |

---

## 2. FUNCTION_REFERENCE.md 수정 (코드 변경 반영)

### 코드 수정에 따른 문서 업데이트

| 서비스 | 수정 내용 |
|--------|-----------|
| `kis_service.py` | `_load_cached_token/real`: 파일→DB 캐시 설명 변경 |
| `kis_service.py` | `_request_new_token/real`: 파일 저장→DB 저장 설명 변경 |
| `kis_service.py` | `get_access_token`: "메모리→파일" → "메모리→DB" |
| `kis_service.py` | `get_overseas_balance`: "여러 TR ID 시도" → DB 조회 (해외주식_잔고조회, 해외주식_잔고조회_종합) |
| `kis_service.py` | `get_overseas_available_cash`: DB TR ID 조회, AAPL 제거 후 early return 설명 추가 |
| `stock_meta_service.py` | `init_api_tr_meta`: 21개 → 24개 (잔고조회, 잔고조회_종합, 가용현금조회 추가) |
| `kis_fetcher.py` | `fetch_overseas_daily_price`: legacy_tr_id DB 조회 파라미터 추가 |
| `kis_fetcher.py` | `_build_overseas_daily_params`: `legacy_tr_id: str=""` 파라미터 추가 |
| `position_service.py` | `_handle_forced_sell`: `forced_qty=holding.quantity` 전달로 설명 수정 |
| `execution_service_v2.py` | `_execute_sell_order`: forced_qty 경로 msg `"forced_sell(full)"` |
| `portfolio_service.py` | `rebalance_portfolio`: None→dict 반환. `_rebalance_logic` 구현 내용 추가 |
| `news_service.py` | placeholder→KIS API 구조 구현 완료. `_fetch_kr_news` 추가 |

---

## 3. FUNCTION_REFERENCE.md 전수 파라미터/반환타입 수정

전체 소스 코드 대조 후 실제 코드와 불일치하는 항목 전수 수정.

### 수정 파일별 주요 변경

| 파일 | 주요 수정 |
|------|-----------|
| `trading_strategy_service.py` | `_run_signals_and_execute`, `_load_macro_and_assets` 등 파라미터 완전 명시 |
| `signal_service.py` | `_score_technical` 파라미터명 정정 (oversold_rsi, overbought_rsi, dip_buy_pct) |
| `position_service.py` | `_process_single_signal` → ExecutionConfig 기반, `_handle_profit_take_signal` 반환타입 bool |
| `execution_service_v2.py` | `_execute_trade_v2`, `_execute_buy_order` "다수" → 완전 시그니처 |
| `report_service.py` | `format_trade_result_report`, `_format_changed_ticker_line` 파라미터 수정 |
| `macro_service.py` | `_calc_technical_20` 반환 `tuple[int, dict, dict]`, `EconomicIndicatorsSnapshot` 타입 명시 |
| `kis_service.py` | `get_real_access_token`, `get_domestic/overseas_trade_history` 등 누락 함수 추가 |
| `kis_fetcher.py` | `fetch_overseas_daily_price`, `_build_overseas_daily_params`, `_try_exchange_fallback` 추가 |
| `market_data_service.py` | `register_ticker(name="")`, `build_watch_item`, `set_tiers` 파라미터 정정 |
| `stock_meta_service.py` | `get_tr_id`, `update_market_code`, `get_exchange_code` 누락 함수 추가 |
| `settings_service.py` | `get_all_settings` 반환타입, `set_setting` 반환타입 수정 |
| `portfolio_service.py` | `sync_with_kis` 반환타입 `List[HoldingSchema]`, `get_usd_cash_balance` 파라미터 추가 |
| `repositories` 전체 | `record`, `query`, `save` 파라미터/반환타입 실제 코드 기준으로 정정 |

---

**작성일**: 2026-03-15
**상태**: ✅ 완료
